class Munchies

  # attr_reader :id,
  #             :destination_city,
  #             :travel_time,
  #             :weather,
  #             :restaurant
  #
  def initialize(munchies_info)
    @id = nil
    # @destination_city = munchies_info["current"])
    # @travel_time = format_daily_weather(forecast_info["daily"])
    # @forecast = format_hourly_weather(forecast_info["hourly"])
    # @restaurant = method_will_go_here(argument)
  end
end
